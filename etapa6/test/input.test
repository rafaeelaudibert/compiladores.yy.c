vec = int[2] : 1 1;
tmp = int : 0;

i = int : 0;
fib = int : 2E; // 46 in decimal

main () = int
{
    print vec[0]
    if (fib <= 1) then
      return 0

    print vec[1]
    if (fib <= 2) then
      return 0
    

    loop (i:3,fib + 1,1) {
      tmp = vec[0] + vec[1]
      vec[0] = vec[1]
      vec[1] = tmp
      print vec[1]
    }
};

